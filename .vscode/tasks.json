{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "dotnet publish",
            "type": "shell",
            "command": "dotnet publish -c Release -r win-x64 --no-self-contained -p:DebugType=None -p:PublishSingleFile=true -o dist src",
			"group": {
				"isDefault": true,
				"kind": "build"
			},
		}
	]
}